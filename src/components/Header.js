import NavBar from './NavBar';

function Header() {
    return (
        <header>
            <NavBar/>
            <div>Add header components later.</div>
        </header>
    );
}

export default Header;